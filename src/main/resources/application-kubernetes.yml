import-path: /etc/.credentials

management:
  endpoint:
    health:
      probes:
        enabled: true

spring:
  main:
    banner-mode: off

lego:
  minio:
    url: http://minio.minio-dev.svc.cluster.local:9000