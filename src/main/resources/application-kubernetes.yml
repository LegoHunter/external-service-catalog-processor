import-path: /etc/.credentials

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

spring:
  main:
    banner-mode: off
  kafka:
    bootstrap-servers: redpanda-dev.redpanda-dev.svc.cluster.local:9092

lego:
  minio:
    url: http://minio.minio-dev.svc.cluster.local