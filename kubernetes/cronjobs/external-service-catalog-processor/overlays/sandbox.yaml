apiVersion: batch/v1
kind: CronJob
metadata:
  name: external-service-catalog-processor
spec:
  schedule: "*/3 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: processor
              env:
                - name: SPRING_PROFILES_ACTIVE
                  value: sandbox,kubernetes
              resources:
                requests:
                  memory: 256Mi
                limits:
                  memory: 512Mi